<%-
base_slurm_args = ["--nodes=1", "--partition=interactive"]
if want_gpu == "1"
  base_slurm_args += ["--gres=gpu"]
end

-%>

---
batch_connect:
  template: "basic"
  conn_params:
    -  code_server_version

script:
  native:
  <%- base_slurm_args.each do |arg| %>
    - "<%= arg %>"
  <%- end %>
